{
  "name": "@mind/draco-ui",
  "version": "0.0.20",
  "description": "",
  "scripts": {
    "build:api": "npm  --prefix ./api run build:all",
    "serve:api": "npm  --prefix ./api run serve",
    "build:globals": "npm  --prefix ./globals run build",
    "build:styles": "npm  --prefix ./style run build",
    "build:ui": "npm  --prefix ./ui run build",
    "build:cpliboard": "npm  --prefix ./wc/clipboard run build",
    "build:pdfviewer": "npm  --prefix ./wc/pdfviewer run build",
    "serve:ui": "npm  --prefix ./ui run start",
    "build:all": "npm run build:api && npm run build:globals && npm run build:styles && npm run build:ui",
    "release": "./bundle.sh"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ladon-documents/ladon-frontend.git"
  },
  "publishConfig": {
    "registry": "https://nexus.mind-consulting.de/repository/npm-private/"
  }
}
